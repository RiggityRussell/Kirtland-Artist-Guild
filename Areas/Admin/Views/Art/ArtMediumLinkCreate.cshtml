@model Kirtland_Artist_Guild.Models.ArtMediumLink

@{
    ViewData["Title"] = "Add Medium";
}

<h1>Add Medium</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ArtMediumLinkCreate">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ArtID" />
            <div class="form-group">
                <select asp-for="ArtMediumID" class="form-control" asp-items="ViewBag.ArtMediumID"></select>
            </div>
            <div class="form-group mt-2">
                <input type="submit" value="Add" class="btn btn-primary" /> |
                <a class="btn btn-secondary" asp-action="Edit" asp-route-id="@ViewBag.art">Back</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}